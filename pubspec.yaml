name: panda_flutter_ui
description: Custom Panda UI components for Flutter's project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  flutter_svg: ^0.23.0+1
  flutter_spinkit: ^5.0.0
  number_display: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter: